# Copy to .env.local for local development
# DATABASE_URL is read from environment - if not set, defaults to SQLite

# Local PostgreSQL (Docker) - optional
# DATABASE_URL="postgresql://aave:aave@localhost:5432/aave_risk"

# Remote PostgreSQL (Neon) - for production
# DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"

# The Graph API key (https://thegraph.com/studio/)
SUBGRAPH_API_KEY="your-api-key-here"
